<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
        <%= @user.dice_point %><br>
        <%= @user.profile %><br>
        <%= link_to 'ポイントのログ', users_point_logs_path(user_id: @user.id) %>

      </h1>
    </section>
  </aside>
</div>